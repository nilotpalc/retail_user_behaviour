[project]
name = "retail-user-behaviour"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "ipython",
    "matplotlib",
    "numpy",
    "pandas",
    "scikit-learn",
    "seaborn",
    "shap",
]

[tool.uv]
package = false
